# Runtime dependencies for the machine fault prediction package.
#
# Keep these aligned with the [project].dependencies section in pyproject.toml.

joblib>=1.3
numpy>=1.24
pandas>=2.0
scikit-learn>=1.4
scipy>=1.10

# Install the local package and CLI in editable mode.
-e .
