# Notebooks

`factory_machine_status_classification.ipynb` is preserved as the original exploratory analysis notebook.

The production workflow now lives in `src/fault_prediction/` and should be used for repeatable training, testing, and prediction. New reusable logic should be added to the package first, then imported into notebooks when exploration needs it.
